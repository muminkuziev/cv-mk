services:
  - type: web
    name: cv-mk
    env: python
    plan: free
    buildCommand: apt-get update -y && apt-get install -y fonts-dejavu-core && pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
